# Core ORSO I/O + model language
orsopy @ git+https://github.com/reflectivity/orsopy.git

# Lipid constants + molecular groups (installed from GitHub)
molgroups @ git+https://github.com/reflectometry/molgroups.git

# Units and conversions
pint

# Optional but recommended for nSL calculations (explicit listing ensures consistency)
numpy

# ORSO YAML model parsing (pulled in by orsopy, but listed for clarity)
pyyaml

